[package]
name = "noema-core"
version = "0.1.0"
edition = "2021"
description = "Core logic: indexing, embeddings, LLM (Ollama). No UI—used by CLI or Tauri later."

[dependencies]
directories = "5"
thiserror = "2"
walkdir = "2"
