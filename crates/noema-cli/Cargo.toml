[package]
name = "noema-cli"
version = "0.1.0"
edition = "2021"
description = "CLI entry point for the Noema backend (for dev and testing)."

[dependencies]
clap = { version = "4", features = ["derive"] }
noema-core = { path = "../noema-core" }
